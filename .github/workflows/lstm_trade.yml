name: LSTM Strategy Trade

# DISABLED: Now handled by AWS Lambda
# Re-enable by uncommenting the schedule below
on:
  # schedule:
  #   - cron: '45 21 * * 1-5'  # Mon-Fri 9:45 PM UTC
  workflow_dispatch:

jobs:
  trade:
    uses: ./.github/workflows/strategy-trade.yml
    with:
      strategy: lstm
