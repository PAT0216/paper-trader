name: Momentum Strategy Trade

on:
  schedule:
    - cron: '0 22 1-3 * *'  # 1st-3rd of month, 10 PM UTC
  workflow_dispatch:

jobs:
  trade:
    uses: ./.github/workflows/strategy-trade.yml
    with:
      strategy: momentum
